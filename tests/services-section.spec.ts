// Services section was removed from the site.
// This file is intentionally empty — kept as a placeholder.
import { test } from '@playwright/test';

test.describe('Services Section (removed)', () => {
  // No tests — services section no longer exists.
});
